import styled from "styled-components";
import { Color } from "../../constants";

export const TableTitle = styled.h4`
  font-size: 2.25em;
  color: ${Color.primary};
  margin-left: 0.25rem;

  padding-top: 0.25rem;
`;
